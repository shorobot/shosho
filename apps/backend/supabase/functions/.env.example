# Local `supabase functions serve --env-file supabase/functions/.env` — never commit real values.
# Stripe TEST keys only (sk_test_…, whsec_… from `stripe listen`). Staging/prod values live in
# Supabase function secrets, set by migrate-staging.yml from GitHub environment secrets.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
