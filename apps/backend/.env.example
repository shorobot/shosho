# apps/backend — only needed to point tests / scripts at a non-local Supabase.
# Local `supabase start` needs nothing: tests read keys from `supabase status`.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
