# apps/backoffice — copy to .env.local (never commit the real file).
# Supabase project the back-office talks to: anon key + a staff auth session (RLS does the guarding).
# Never put the service-role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
