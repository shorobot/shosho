# SHOSHO

Platform: guest site + ordering + back-office + AI-agent layer.

Developed by a team of Claude Code sessions coordinated by an Orchestrator session (S0, CTO).

**Every new session starts by reading:**
1. `/memory/state.md` — what is done, what is in progress
2. `/memory/decisions.md` — why
3. `/memory/sessions.md` — session roster and ID format
4. its own boot in `/memory/boots/S<N>-<NN>-<slug>.md`

Layout: `/memory` — working memory, `/docs` — architecture and contracts, `/apps` — code.
Repository language: English (D-006).
